name: Build Elsewhere APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.0'
          channel: 'stable'
      - name: Create full Flutter project
        run: |
          flutter create --org com.drrandom.elsewhere --project-name elsewhere /tmp/newproject
          cp -r /tmp/newproject/android $GITHUB_WORKSPACE/android
          cp -r /tmp/newproject/ios $GITHUB_WORKSPACE/ios
      - run: flutter pub get
      - name: Build APK
        run: flutter build apk --debug
      - uses: actions/upload-artifact@v4
        with:
          name: elsewhere-debug.apk
          path: build/app/outputs/flutter-apk/app-debug.apk                      android:launchMode="singleTop"
                      android:taskAffinity=""
                      android:theme="@style/LaunchTheme"
                      android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
                      android:hardwareAccelerated="true"
                      android:windowSoftInputMode="adjustResize">
                      <meta-data
                        android:name="io.flutter.embedding.android.NormalTheme"
                        android:resource="@style/NormalTheme"/>
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN"/>
                          <category android:name="android.intent.category.LAUNCHER"/>
                      </intent-filter>
                  </activity>
                  <meta-data
                      android:name="flutterEmbedding"
                      android:value="2"/>
              </application>
              <uses-permission android:name="android.permission.INTERNET"/>
              <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
              <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
          </manifest>
          EOF
      - run: flutter pub get
      - name: Build APK
        run: yes | flutter build apk --debug
      - uses: actions/upload-artifact@v4
        with:
          name: elsewhere-debug.apk
          path: build/app/outputs/flutter-apk/app-debug.apk
